<h1><%= @house.name %></h1>
<% @house.students.each do |student| %>
  <a href="/students/<%= student.id %>">
    <div class="student">
      <h2><%= student.name %></h2>
      <img src="<%= student.img_url %>" alt="">
    </div>
  </a>
<% end %>
